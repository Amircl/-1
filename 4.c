#include <stdio.h>

int main() {
    int n;

    // Ввод числа
    printf("Введите число: ");
    scanf("%d", &n);

    // Проверка, является ли число степенью двойки
    // (n > 0) исключает 0 и отрицательные числа
    // (n & (n - 1)) == 0 проверяет наличие только одной единицы в битовом представлении
    int is_power = (n > 0) && ((n & (n - 1)) == 0;

    // Вывод результата (1 - true, 0 - false)
    printf("%d\n", is_power);

    return 0;
}
